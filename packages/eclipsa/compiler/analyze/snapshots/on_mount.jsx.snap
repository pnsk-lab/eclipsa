// ================= ENTRY (on_mount.jsx) ==
import { onMount } from 'eclipsa'

export default () => {
  const value = 'mounted'

  onMount(() => {
    console.log(value)
  })

  return <button>{value}</button>
}


// ================= analyzed ==
import { __eclipsaComponent } from "eclipsa/internal";
import { onMount } from 'eclipsa'

export default __eclipsaComponent(() => {
  const value = 'mounted'

  onMount(() => {
    console.log(value)
  })

  return <button>{value}</button>
}, "12cvs17", ()=>[], undefined, { optimizedRoot: true })


// ================= 12cvs17 (component) ==
import { onMount } from 'eclipsa'
export default (__scope) => {
  const value = 'mounted'

  onMount(() => {
    console.log(value)
  })

  return <button>{value}</button>
};
